const initialState = [
  {
    link: "/",
    lable: "home"
  },
  {
    link: "/tracks",
    lable: "Tracks"
  },
  {
    link: "/addtracks",
    lable: "Add-Tracks"
  }
];

export default function menu(state = initialState) {
  return state;
}